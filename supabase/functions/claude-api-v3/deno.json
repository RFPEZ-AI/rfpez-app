{
  "compilerOptions": {
    "lib": ["deno.window"],
    "strict": true
  },

  "test": {
    "include": ["**/*test.ts", "**/*test.js", "tests/**/*.ts", "tests/**/*.js"]
  },
  "tasks": {
    "test": "deno test --allow-all",
    "test:watch": "deno test --allow-all --watch",
    "test:coverage": "deno test --allow-all --coverage=coverage",
    "coverage": "deno coverage coverage",
    "dev": "deno run --allow-all",
    "serve": "deno run --allow-all --watch"
  },
  "lint": {
    "exclude": ["backup/"]
  },
  "imports": {
    "std/": "https://deno.land/std@0.168.0/",
    "supabase": "https://esm.sh/@supabase/supabase-js@2",
    "@aws-sdk/client-bedrock-runtime": "https://esm.sh/@aws-sdk/client-bedrock-runtime@3.682.0",
    "@aws-sdk/credential-providers": "https://esm.sh/@aws-sdk/credential-providers@3.682.0"
  }
}