// Test runner to manually check if our test setup is working
import { describe, it, expect } from '@jest/globals';

describe('Basic test', () => {
  it('should pass a simple test', () => {
    expect(1 + 1).toBe(2);
  });
});

console.log('Test runner script loaded successfully');
