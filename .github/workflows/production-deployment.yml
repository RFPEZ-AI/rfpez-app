name: Production Deployment

on:
  push:
    branches: [ master ]
  workflow_dispatch:

env:
  NODE_VERSION: '18.x'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    name: Build and Deploy to Production
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: true

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'

    - name: Install dependencies
      run: |
        npm ci
        npm run mcp:install

    - name: Run tests
      run: |
        npm test -- --coverage --watchAll=false --passWithNoTests
      env:
        CI: true

    - name: Build application
      run: npm run build
      env:
        CI: true
        REACT_APP_SUPABASE_URL: ${{ secrets.REACT_APP_SUPABASE_URL_PRODUCTION }}
        REACT_APP_SUPABASE_ANON_KEY: ${{ secrets.REACT_APP_SUPABASE_ANON_KEY_PRODUCTION }}
        REACT_APP_CLAUDE_API_KEY: ${{ secrets.REACT_APP_CLAUDE_API_KEY_PRODUCTION }}
        REACT_APP_PRODUCTION_URL: https://rfpez.ai
        GENERATE_SOURCEMAP: false

    - name: Deploy to Azure Static Web Apps
      id: builddeploy
      uses: Azure/static-web-apps-deploy@v1
      with:
        azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_PRODUCTION }}
        repo_token: ${{ secrets.GITHUB_TOKEN }}
        action: "upload"
        app_location: "/" 
        api_location: ""
        output_location: "build"
        skip_app_build: true

    - name: Deploy Supabase Edge Functions
      run: |
        npm install -g @supabase/cli
        supabase functions deploy mcp-server --project-ref ${{ secrets.SUPABASE_PROJECT_REF_PRODUCTION }}
        supabase functions deploy claude-api --project-ref ${{ secrets.SUPABASE_PROJECT_REF_PRODUCTION }}
      env:
        SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN_PRODUCTION }}

    - name: Run post-deployment health checks
      run: |
        # Wait for deployment to be available
        sleep 30
        
        # Basic health check - verify the app loads
        curl -f https://rfpez.ai || exit 1
        
        # Verify static assets are accessible
        curl -f https://rfpez.ai/static/css/ || echo "CSS assets check failed"
        curl -f https://rfpez.ai/static/js/ || echo "JS assets check failed"
        
        echo "Health checks completed successfully"

    - name: Notify deployment status
      if: always()
      run: |
        if [ "${{ job.status }}" == "success" ]; then
          echo "✅ Production deployment successful to https://rfpez.ai"
        else
          echo "❌ Production deployment failed"
          exit 1
        fi